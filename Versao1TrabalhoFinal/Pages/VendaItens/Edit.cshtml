@page "{id:int}"
@model Versao1TrabalhoFinal.Pages.VendaItens.EditModel
@{
    ViewData["Title"] = "Editar Item de Venda";
}

<div class="row justify-content-center">
    <div class="col-lg-7">
        <div class="card shadow-sm">
            <div class="card-body">
                <h2 class="mb-4">Editar item de venda</h2>

                <form method="post">
                    <input type="hidden" asp-for="VendaItem.Id" />

                    <div class="mb-3">
                        <label asp-for="VendaItem.VendaId" class="form-label">Venda</label>
                        <select asp-for="VendaItem.VendaId" asp-items="Model.VendasSelect" class="form-select"></select>
                    </div>

                    <div class="mb-3">
                        <label asp-for="VendaItem.ProdutoId" class="form-label">Produto</label>
                        <select asp-for="VendaItem.ProdutoId" asp-items="Model.ProdutosSelect" class="form-select"></select>
                    </div>

                    <div class="mb-3">
                        <label asp-for="VendaItem.Quantidade" class="form-label"></label>
                        <input asp-for="VendaItem.Quantidade" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="VendaItem.Preco" class="form-label"></label>
                        <input asp-for="VendaItem.Preco" class="form-control" />
                    </div>

                    <button type="submit" class="btn btn-primary">Guardar alterações</button>
                    <a asp-page="./Index" class="btn btn-outline-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>
