@page "{id:int}"
@model Versao1TrabalhoFinal.Pages.VendaItens.DeleteModel
@{
    ViewData["Title"] = "Eliminar Item de Venda";
}

<div class="card shadow-sm">
    <div class="card-body">
        <h2 class="text-danger">Eliminar item de venda</h2>
        <p>Tens a certeza que queres eliminar este item?</p>

        <dl class="row">
            <dt class="col-sm-3">Produto</dt>
            <dd class="col-sm-9">@Model.VendaItem.Produto?.Nome</dd>

            <dt class="col-sm-3">Quantidade</dt>
            <dd class="col-sm-9">@Model.VendaItem.Quantidade</dd>
        </dl>

        <form method="post">
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <a asp-page="./Index" class="btn btn-outline-secondary">Cancelar</a>
        </form>
    </div>
</div>
