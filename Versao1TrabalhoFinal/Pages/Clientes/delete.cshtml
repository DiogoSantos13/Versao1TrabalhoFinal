@page "{id:int}"
@model Versao1TrabalhoFinal.Pages.Clientes.DeleteModel
@{
    ViewData["Title"] = "Eliminar Cliente";
}

<div class="card shadow-sm">
    <div class="card-body">
        <h2 class="text-danger">Eliminar cliente</h2>
        <p>Tens a certeza que queres eliminar este cliente?</p>

        <dl class="row">
            <dt class="col-sm-2">Nome</dt>
            <dd class="col-sm-10">@Model.Cliente.Nome</dd>

            <dt class="col-sm-2">Email</dt>
            <dd class="col-sm-10">@Model.Cliente.Email</dd>
        </dl>

        <form method="post">
            <button type="submit" class="btn btn-danger">Eliminar</button>
            <a asp-page="./Index" class="btn btn-outline-secondary">Cancelar</a>
        </form>
    </div>
</div>
